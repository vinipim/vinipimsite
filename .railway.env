# Railway-specific configuration
# This file ensures Railway uses the correct settings for deployment

# Build optimization
RAILWAY_BUILD_COMMAND="pnpm install --frozen-lockfile && pnpm build"
RAILWAY_START_COMMAND="node dist/server/index.cjs"

# Health check configuration
RAILWAY_HEALTHCHECK_PATH="/health"
RAILWAY_HEALTHCHECK_TIMEOUT=60

# Environment
NODE_ENV=production
PORT=8080
# Replace with the connection string provided by your Railway database service
DATABASE_URL="REPLACE_WITH_RAILWAY_DATABASE_URL"

# Static file caching
RAILWAY_STATIC_CACHE=true
