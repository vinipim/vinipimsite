{
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs", "pnpm"]
    },
    "install": {
      "cmds": ["pnpm install --frozen-lockfile"]
    },
    "build": {
      "cmds": ["pnpm build"]
    }
  },
  "start": {
    "cmd": "node dist/server/index.cjs"
  }
}
